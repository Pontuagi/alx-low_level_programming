#include "main.h"
#include <string.h>

/**
  * _strcmp - compare two strings
  * @s1: string 1
  * @s2: string 2
  * Return: 1 if same and 0 if different
  */

int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);
	return (i);
}
