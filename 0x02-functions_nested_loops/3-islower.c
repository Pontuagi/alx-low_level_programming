#include "main.h"
#include <ctype.h>

/**
  * _islower - function to check if a letter is lowercase
  *Return: return 1 if c is lowercase otherwise 0
  */

int _islower(int c)
{
	int c;

	int i = islower(c);
	return (i);
}
