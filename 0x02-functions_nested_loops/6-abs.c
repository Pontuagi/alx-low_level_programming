#include "main.h"
#include <stdio.h>

/**
  * _abs - entry to our function
  * Return: 1 or 0
  * @int: value to check absolute value of
  */

int _abs(int)
{
	int r;
	int j = abs(r);

	return (j);
}
