#include "main.h"
#include <string.h>

/**
  * _strlen - function to return the length of a string
  * @s: string whose legth to check
  * Return: return length of the string
  */

int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
