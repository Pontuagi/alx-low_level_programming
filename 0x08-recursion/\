#include "main.h"

/**
  * _puts_recursion - output a string
  * @s: character to print
  */

void _puts_recursion(char *s)
{
	int t;

	if (*(s + t) == '\0')
		return;
	_putchar(s[t]);
	_puts_recursion(s);
}
